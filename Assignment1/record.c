// record.c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "record.h"

// Creates and returns a new record
Record create_record() {

}

// Compares two records. Returns 1 the first is larger, 0 if they are the same, and -1 if the first is smaller 
int compare_records(Record* self, Record other) {

}

// Prints the identifier of a record, and its data if available
void print_record(Record* self) {

}

// Tests all of the above functions
void test_record(Record* self) {

}